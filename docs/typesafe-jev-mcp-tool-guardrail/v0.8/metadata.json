{
  "name": "typesafe-jev-mcp-tool-guardrail",
  "displayName": "TypeSafe Jev MCP Tool Guardrail",
  "version": "0.8",
  "provider": "WSO2",
  "categories": [
    "MCP",
    "Security"
  ],
  "description": "Screens MCP tools/call requests using TypeSafe AI's Jev System One model.\nSends the tool name and arguments to Jev with a configurable battery of typed questions (calibrated yes/no, a position on an operator-defined scale, or a choice between operator-defined options) and blocks the call with a JSON-RPC error when any question's answer crosses its threshold, or only records the hit in monitor mode.\nThe default battery asks whether the call is destructive, irreversible, or sends private data out, plus whether it falls outside a configured scope when one is set.\nOther MCP methods pass through; a request body that cannot be read unambiguously is rejected so the policy always screens the call the MCP server will run."
}
